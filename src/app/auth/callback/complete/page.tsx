import AuthCallbackClient from '../client-page';

/**
 * OAuth callback completion page
 * This page handles the actual token processing client-side
 */
export default function AuthCallbackCompletePage() {
  return <AuthCallbackClient />;
}
